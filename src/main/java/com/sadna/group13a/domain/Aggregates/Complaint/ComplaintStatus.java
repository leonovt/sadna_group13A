package com.sadna.group13a.domain.Aggregates.Complaint;

/** Lifecycle of a buyer complaint to the platform administrators (II.3.3 / II.6.3). */
public enum ComplaintStatus {
    OPEN,
    RESOLVED
}
